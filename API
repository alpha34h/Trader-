from flask import Flask, jsonify

app = Flask(__name__)

# هذا هو المسار الذي تحاول الاتصال به
@app.route('/start', methods=['GET'])
def start():
    return jsonify({"status": "active", "price": 2350.0})

# هذا السطر مهم جداً لـ Vercel
app.debug = True
